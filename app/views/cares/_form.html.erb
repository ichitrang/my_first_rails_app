<%= form_with(model: care) do |form| %>
  <% if care.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(care.errors.count, "error") %> prohibited this care from being saved:</h2>

      <ul>
        <% care.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :make, style: "display: block" %>
    <%= form.text_field :make %>
  </div>

  <div>
    <%= form.label :model, style: "display: block" %>
    <%= form.text_field :model %>
  </div>

  <div>
    <%= form.label :year, style: "display: block" %>
    <%= form.number_field :year %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
